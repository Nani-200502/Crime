# If empty, frontend uses same-origin API paths (recommended when served by Flask)
VITE_API_BASE_URL=

# Used by Vite dev proxy when running frontend standalone via `npm run dev`
VITE_BACKEND_ORIGIN=http://127.0.0.1:5000
